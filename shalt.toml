# shalt workspace configuration
[project]
name = "sje"
stack = "rust"

[zones]
steps = "tests"
src = "src"

[runner]
command = "cargo test --test shalt"
format = "cucumber-json"
report = ".shalt/cucumber.json"
timeout = 900

[runner.env]
SHALT_REPORT = "{report}"

[ui]
editor = ""
color = "auto"
